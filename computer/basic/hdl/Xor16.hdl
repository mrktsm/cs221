CHIP Xor16 {
    IN a[16], b[16];
    OUT out[16];

    PARTS:
    Nand16(a=a, b=b, out=nand1);
    Nand16(a=a, b=nand1, out=nand2);
    Nand16(a=b, b=nand1, out=nand3);
    Nand16(a=nand2, b=nand3, out=out);
}
